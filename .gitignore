.DS_Store
*.logs
node_modules/
.vscode/
# Ignore local environment files
# See .env at project root for VITE_ variables
.env

# Other files to ignore (even if we dont have some, just to be safe)
# Build outputs
# /dist/ exluded until i get confirmation from the teacher to put it here.
# on that note, after that dont forget to update read me again in ALL sections.
# Maybe take some of the stuff from index.html and index.js for main.html and main.js?
.firebase/

# Vite / build caches
.vite/
.cache/

# Local env overrides
.env.local
.env.*.local

# OS / editor / logs
Thumbs.db
*.log
npm-debug.log*
.idea/
.history/

# Test / coverage
coverage/
test-output/

# Optional generated files
*.map
